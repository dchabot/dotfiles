- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']
  
- link:
    ~/.dotfiles: ''
    ~/.bash_profile: bash_profile
    ~/.bashrc: bashrc
    ~/.bash_aliases: bash_aliases
    ~/.gitconfig: gitconfig
    ~/.vimrc: vimrc
    ~/.vim: vim
    ~/.tmux: tmux
    ~/.tmux.conf: tmux.conf
    ~/.tmuxline.conf: tmuxline.conf
    ~/.ssh/config: ssh/config
        
- shell:
    - git submodule update --init --recursive fonts
    - cd fonts && ./install.sh
    - git submodule update --init --recursive tmux/plugins/tpm
    - tmux source ~/.tmux.conf
# Revisit Homebrew (linuxbrew) installation later
# See here for deets - https://docs.brew.sh/Homebrew-on-Linux
#
#    - git clone git://github.com/Homebrew/brew ~/.linuxbrew/Homebrew
#    - mkdir ~/.linuxbrew/bin
#    - ln -s ~/.linuxbrew/Homebrew/bin/brew ~/.linuxbrew/bin
#    - eval $(~/.linuxbrew/bin/brew shellenv)
