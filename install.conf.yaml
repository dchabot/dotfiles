- clean: ['~']
- link:
    ~/.dotfiles: ''
    ~/.bashrc: bashrc
    ~/.bash_aliases: bash_aliases
    ~/.hgrc: hgrc
    ~/.gitconfig: gitconfig
    ~/.gbp.conf: gbp.conf
    ~/.dput.cf: dput.cf
    ~/.vimrc: vimrc
    ~/.vim: vim
    ~/.tmux.conf: tmux.conf
    ~/.tmuxline.conf: tmuxline.conf
    ~/.condarc: condarc
    ~/.ssh: ssh
    ~/.linuxbrew: /nfs/dchabot/.linuxbrew
- shell:
    - [git submodule update --init --recursive, Installing submodules]
