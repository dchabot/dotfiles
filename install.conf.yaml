- clean: ['~']
- link:
    ~/.dotfiles: ''
    ~/.bashrc: bashrc
    ~/.bash_profile: bash_profile
    ~/.bash_aliases: bash_aliases
    ~/.hgrc: hgrc
    ~/.gitconfig: gitconfig
    ~/.vimrc: vimrc
    ~/.vim: vim
    ~/.tmux.conf: tmux.conf
    ~/.tmuxline.conf: tmuxline.conf
    ~/.ssh: ssh
    ~/.config/nvim: vim
- shell: []
